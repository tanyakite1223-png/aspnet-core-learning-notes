# 認證與授權筆記:Authentication vs Authorization 概念

## 核心概念

### Authentication（驗證）
- **是什麼**：確認「你是誰」（身份確認）
- **情境**：輸入帳號密碼登入，系統確認「這個人是 Amber」
- **失敗結果**：帳號或密碼錯誤，擋在門外，根本進不到系統

### Authorization（授權）
- **是什麼**：判斷「你能做什麼」（權限判斷）
- **情境**：登入後試圖存取「主管審核」頁面，系統擋下來說「你沒有權限」
- **失敗結果**：身份已確認，但沒有對應權限，無法執行該操作

### 順序關係
必須先通過 **Authentication**，才能進行 **Authorization** 的判斷。
沒有身份，談不上權限。

> 記憶技巧：**Authen**tication 的 "n" 聯想到 "name"——確認名字（身份）

---

## HTTP 無狀態與 Cookie

HTTP 本身是**無狀態**的——每個請求對伺服器來說都是陌生人，不會自動記得你上一次做了什麼。

### Cookie 的角色（通行證）
1. 使用者輸入帳號密碼，按下登入
2. 伺服器確認身份成功，發出一張「通行證」給瀏覽器
3. 瀏覽器將通行證存在 Cookie 裡
4. 之後每次發請求，瀏覽器**自動**帶上這張通行證
5. 伺服器看到通行證，就知道「這是已登入的 Amber」

---

## 兩種驗證方式的適用情境

| 方式 | 適用情境 | 原因 |
|------|----------|------|
| **Cookie-based 驗證** | 網頁應用（MVC） | 瀏覽器會自動帶上 Cookie，不需工程師額外處理 |
| **JWT Token 驗證** | API / 手機 App | 沒有瀏覽器自動帶 Cookie 的機制，需在每個請求的 Header 手動附上 Token |

> ExpenseSystem 裡兩者都會用到：MVC 頁面用 Cookie-based，Web API 用 JWT Token。

