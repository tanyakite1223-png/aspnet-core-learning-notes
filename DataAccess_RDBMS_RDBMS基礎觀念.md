# 資料存取筆記:RDBMS 觀念 — Table、Column、Row、Primary Key、Foreign Key

> 學習日期:2026-04-29
> 主題層級:資料存取 → SQL 與 RDBMS 基礎 → 第 1 個主題

---

## 一、為什麼要先學 RDBMS 觀念?

在進入 EF Core 和 LINQ 之前,必須先理解資料庫底層的運作方式。**EF Core 只是一層包裝**,它最終把 C# 程式碼翻譯成 SQL 指令送到資料庫執行。

如果不懂底層,出問題時會無法判斷:
- 是 EF Core 寫錯?
- 還是資料庫設計有問題?
- 還是 SQL 本身效能差?

> **關鍵心法:資料庫不會主動幫你判斷意義,它只會老實儲存。**

---

## 二、基本術語:Table / Column / Row

### 範例表(會員資料)

| 會員ID | 姓名 | Email | 電話 |
|---|---|---|---|
| 1 | 王小明 | ming@gmail.com | 0912-345-678 |
| 2 | 林美麗 | mei@yahoo.com | 0987-654-321 |
| 3 | 陳大華 | hua@hotmail.com | 0922-111-222 |

| 術語 | 中文 | 說明 |
|---|---|---|
| **Table** | 資料表 | 整張表 |
| **Column** | 欄(直的) | 「姓名」、「Email」這些欄位定義 |
| **Row** | 列(橫的) | 一筆完整的資料(也叫 Record) |

📌 **記憶技巧**:
- **Column** 像「圓**柱**(柱子)」→ **直**的
- **Row** 像「划船的一**排**槳」→ **橫**的
- 或是:Excel 上方 A、B、C... 是 Column;左邊 1、2、3... 是 Row

---

## 三、Primary Key(主鍵 / PK)

### 定義

> **用來唯一識別每一筆 Row 的欄位**

### 三大特性

| 特性 | 英文 | 說明 |
|---|---|---|
| **唯一性** | Unique | 整張表中,這個欄位的值不能重複 |
| **非空** | Not Null | 不能是空值 |
| **不可變** | Immutable | 一旦給定,通常不會去改它 |

### 為什麼不用「姓名」當 PK?

| 欄位 | 違反的特性 |
|---|---|
| 姓名 | 唯一性(同名同姓)、不可變(會改名) |
| 部門 | 唯一性(同部門很多人)、不可變(會調部門) |
| 薪水 | 唯一性(可能相同)、不可變(會調薪) |

### 業界慣例

> 實務上,PK 通常用一個**跟業務無關的「流水號」**,例如 1、2、3...

這種 ID 叫做 **Auto Increment(自動遞增)**——新增資料時不用自己給,資料庫會自動發號碼。

```
員工編號(E001、E002...)
訂單編號(1001、1002...)
商品ID(101、102...)
```

這些 ID 本身**沒有任何業務意義**,純粹是身份證字號的角色——這也是業界主流設計。

---

## 四、Foreign Key(外鍵 / FK)

### 定義

> **用來「指向(參照)」另一張表的某一筆 Row 的欄位**

### 範例:訂單表

| 訂單ID(PK) | 會員ID(FK→會員表) | 商品ID(FK→商品表) | 訂購日期 |
|---|---|---|---|
| 1001 | 1 | 101 | 2026-04-29 |
| 1002 | 1 | 102 | 2026-04-29 |
| 1003 | 2 | 101 | 2026-04-30 |

### PK vs FK 對照表

| | Primary Key | Foreign Key |
|---|---|---|
| **中文** | 主鍵 | 外鍵 |
| **角色** | 唯一識別自己這張表的每一筆 Row | 指向另一張表的某一筆 Row |
| **是否唯一** | 一定唯一 | **可以重複**(王小明可下很多筆訂單) |
| **是否可空** | 不能空 | 通常可空(看設計) |

### 為什麼要用 FK 而不是把資料複製過去?

如果把「王小明、ming@gmail.com、0912-345-678」整筆資料複製到訂單表,會造成:

1. **資料冗餘(Data Redundancy)**:同樣的資料重複存,佔空間
2. **維護困難**:王小明改 Email 時,要改很多地方
3. **資料不一致(Data Inconsistency)**:如果漏改一筆,同一個人會出現兩個 Email,資料就「壞掉」了

> **關聯式資料庫的核心原則:每一份資料只存一份,在需要的地方用 ID 去參照它。**
>
> 這個原則叫做 **Normalization(正規化)**。

---

## 五、PK 與 FK 的對稱關係(超重要!)

| 角色 | 在自己的表裡 |
|---|---|
| PK | **只出現 1 次**(作者ID=2 在作者表只有 1 筆) |
| FK | **可以出現多次**(作者ID=2 在書籍表可能出現 N 次) |

> **「FK 出現多次」這件事,就是「一對多關聯」的真實樣貌。**

### 範例

```
作者表(Authors)         書籍表(Books)
+--------+              +----------+
| ID = 2 |  ←─────────  | 作者ID=2 |  (挪威的森林)
| 村上春樹|  ←─────────  | 作者ID=2 |  (1Q84)
+--------+              +----------+
   1 筆                    2 筆
```

業務意義:**村上春樹寫了 2 本書**(一對多)

---

## 六、NULL(空值)

### 定義

> **NULL = 「這個欄位沒有值」/「未知」/「不適用」**

### NULL 不等於什麼?

| 看起來像,但其實不一樣 | 為什麼不一樣 |
|---|---|
| 空字串 `""` | 有一個盒子,內容是空的(長度 0) |
| 數字 `0` | 0 是一個明確的數值 |
| 日期 `1900-01-01` | 這是一個有效的日期,跟「沒填」是兩回事 |

> **空字串 vs NULL 的比喻**
> - 空字串:有盒子,盒子是空的
> - NULL:連盒子都沒有

### 為什麼不能用假值代替 NULL?

如果用 `1900-01-01` 代替「沒填生日」,之後就**無法區分**:
- 真的有會員生日是 1900 年
- 沒填生日所以塞 1900-01-01

→ 資料的「真實狀態」和「填寫狀態」混在一起,業務上就無法判斷。

### NULL 在 SQL 裡的特殊行為

> **NULL 不能用 `=` 來比較**

```sql
-- ❌ 抓不到任何資料
SELECT * FROM Members WHERE 生日 = NULL

-- ✅ 正確寫法
SELECT * FROM Members WHERE 生日 IS NULL

-- ✅ 找出有填生日的會員
SELECT * FROM Members WHERE 生日 IS NOT NULL
```

📌 **為什麼?** 因為 NULL 代表「未知」,「未知 = 未知」這個比較本身結果也是「未知」,不是 true。SQL 用 `IS NULL` / `IS NOT NULL` 這個專門的語法處理。

---

## 七、欄位是否允許 NULL — 業界慣例

| 欄位類型 | 通常設定 |
|---|---|
| **主鍵(PK)** | 一律 NOT NULL |
| **外鍵(FK)** | 看「這筆資料是否一定要有對應」(例如:訂單一定要有會員 → NOT NULL) |
| **必填欄位**(姓名、Email、密碼...) | NOT NULL |
| **可選欄位**(生日、備註、頭像...) | 允許 NULL |
| **數值欄位**(庫存、金額...) | 看業務是否需要區分「0」和「未知」 |

### 重要設計思維

> **設計欄位前,要先想清楚業務上需不需要區分這個狀態**——這就是資料庫設計的精髓。

舉例:商品表的「庫存」

| 設計選擇 | 業務語意 |
|---|---|
| NOT NULL,預設 0 | 不區分「賣完」和「還沒進貨」(都用 0 表示) |
| 允許 NULL | 賣完 = 0,沒進貨 = NULL(語意清楚但程式要多處理 NULL) |

兩種設計都對,**重點是業務需求**。

---

## 八、業務意義(Business Meaning)

> **業務意義 = 把資料庫裡的數字,翻譯回人話。**

| 資料庫上的事實 | 業務意義(翻成人話) |
|---|---|
| 訂單表「會員ID = 1」出現 5 次 | 王小明這個會員下了 5 筆訂單 |
| 商品表「庫存 = 0」 | 這項商品賣完了,目前缺貨 |
| 會員表「Email 欄位是 NULL」 | 這個會員還沒填 Email |

實務上會聽到的用法:
- 「這個欄位的業務意義是什麼?」
- 「這個欄位的命名沒有反映出業務意義」
- 「先把業務需求弄清楚,再來設計 Table」

→ **資料庫設計第一步永遠是:先理解業務需求,再來設計結構。**

---

## 九、命名規則(實務版)

### 9.1 命名語言

| 情境 | 建議 |
|---|---|
| 學習階段 | 中文、英文都可以 |
| 找工作 / 進公司 | **強烈建議練習英文命名** |
| 接老專案 | 跟著現有風格,不要混用 |

### 9.2 PK 命名

| 寫法 | 範例 | 評價 |
|---|---|---|
| `表名 + ID` | `UserID`、`ArticleID` | ✅ **業界主流,最推薦** |
| 單純 `Id` | `Id` | ✅ EF Core 預設慣例 |
| `表名_ID` | `User_ID` | ⚠️ 舊風格 |

### 9.3 FK 命名 — 業界鐵則

> **FK 的名字 = 它指向的那張表的 PK 名字**

範例:

| 表 | 欄位 | 角色 |
|---|---|---|
| User | UserID | PK |
| Article | UserID | FK(指向 User.UserID) |
| Order | UserID | FK(指向 User.UserID) |

### 9.4 一般欄位命名三原則

1. **用名詞,不要用動詞**(`LoginTime` ✅,`Login` ❌)
2. **清楚 > 簡短**(`OrderStatus` ✅,`St` ❌)
3. **業務語意 > 技術細節**(`IsActive` ✅,`Flag1` ❌)

### 9.5 大小寫格式 — .NET 用 PascalCase

| 風格 | 範例 | 適用 |
|---|---|---|
| **PascalCase** | `UserID`、`CreatedAt` | ✅ **C# / .NET 主流** |
| camelCase | `userID` | JavaScript / Java |
| snake_case | `user_id` | Python |

### 9.6 常用欄位英文對照表

#### 使用者
| 中文 | 英文 |
|---|---|
| 使用者ID | UserID |
| 帳號 | Account / Username |
| 密碼 | Password |
| 姓名 | Name / FullName |
| Email | Email |
| 電話 | Phone / PhoneNumber |
| 生日 | Birthday / BirthDate |
| 自我介紹 | Bio / Introduction |
| 頭像 | Avatar / ProfileImage |

#### 文章
| 中文 | 英文 |
|---|---|
| 文章ID | ArticleID / PostID |
| 標題 | Title |
| 內容 | Content / Body |
| 摘要 | Summary |
| 封面圖 | CoverImage |
| 作者ID | AuthorID |

#### 商品 / 訂單
| 中文 | 英文 |
|---|---|
| 商品ID | ProductID |
| 商品名稱 | ProductName |
| 價格 | Price |
| 庫存 | Stock / Quantity |
| 訂單ID | OrderID |
| 總金額 | TotalAmount |

#### 時間(超常用)
| 中文 | 英文 |
|---|---|
| 建立時間 | CreatedAt / CreatedDate |
| 更新時間 | UpdatedAt / ModifiedDate |
| 刪除時間 | DeletedAt |
| 登入時間 | LoginTime / LastLoginAt |

> 💡 **強烈建議**:幾乎每張表都放 `CreatedAt`(建立時間)——除錯、查問題、做報表時超有用。

#### 布林(Yes/No)
| 中文 | 英文 |
|---|---|
| 是否啟用 | IsActive |
| 是否刪除 | IsDeleted |
| 是否已讀 | IsRead |
| 是否公開 | IsPublic |

> 慣例:布林欄位以 `Is` 開頭(也有 `Has`,例如 `HasAvatar`)。

---

## 十、常見錯誤與正確比較

### 錯誤 1:用業務欄位當 PK

```
❌ 用「姓名」當會員表的 PK     → 同名同姓會撞車,改名也會壞
✅ 用「UserID(自動遞增)」     → 與業務脫鉤,永遠唯一
```

### 錯誤 2:把整筆會員資料複製到訂單表

```
❌ 訂單表存:「王小明、ming@gmail.com、商品名稱、價格...」
   → 會員改 Email 時,要改全部訂單
✅ 訂單表存:「會員ID(FK)、商品ID(FK)」
   → 用 ID 參照,改 Email 只改 1 個地方
```

### 錯誤 3:用空字串或假值代替 NULL

```
❌ 沒填生日 → 存 "1900-01-01"
   → 跟「真的 1900 年生」混在一起,無法區分
✅ 沒填生日 → 存 NULL
   → 用 IS NULL 查詢就能精準分辨
```

### 錯誤 4:用 `=` 比較 NULL

```sql
❌ WHERE 生日 = NULL          -- 抓不到任何資料
✅ WHERE 生日 IS NULL          -- 正確寫法
```

### 錯誤 5:PK 命名不專業

```
❌ 文章表 PK 叫「自動編號」    → 這是「設定」不是「名字」
✅ 文章表 PK 叫 ArticleID      → 業務語意清楚
```

---

## 十一、本主題完整設計練習(部落格系統)

### User Table

| 欄位 | 設定 |
|---|---|
| UserID | PK,NOT NULL,Auto Increment |
| Account | NOT NULL |
| Email | NOT NULL |
| Password | NOT NULL |
| Bio | 允許 NULL |

### Article Table

| 欄位 | 設定 |
|---|---|
| ArticleID | PK,NOT NULL,Auto Increment |
| UserID | FK → User.UserID,NOT NULL |
| Title | NOT NULL |
| Content | NOT NULL |
| CoverImage | 允許 NULL |

### 設計重點回顧

1. ✅ 兩張表都有 PK
2. ✅ 文章表用 FK 指向 User Table
3. ✅ FK 命名跟它指向的 PK 同名(都叫 UserID)
4. ✅ 必填欄位 NOT NULL,選填欄位允許 NULL
5. ✅ FK(UserID)設 NOT NULL,因為「每篇文章一定要有作者」

---

## 十二、進階觀念(知道有這個東西就好)

### Composite Key(複合主鍵)

一張表只能有 **1 個 PK**,但這個 PK 可以由**多個欄位組成**。

範例:課程選修表

| 學生ID | 課程ID | 選修日期 |
|---|---|---|
| S001 | C101 | 2026-09-01 |
| S001 | C102 | 2026-09-01 |
| S002 | C101 | 2026-09-02 |

- 「學生ID」會重複(同一個學生選好幾門課)
- 「課程ID」會重複(同一門課很多人選)
- 但「**學生ID + 課程ID**」合在一起是唯一的

→ PK 設成 `(學生ID, 課程ID)` 兩個欄位合起來 = **Composite Key(複合主鍵)**

📌 觀念釐清:
- 一張表只能有 **1 個 PK** ✅
- 但這個 PK 可以由 **多個欄位** 組成 ✅
- 「多欄位組成的 PK」 ≠ 「多個 PK」

---

## 十三、本主題核心心法總結

1. 🔑 **資料庫不會主動判斷意義,設計時要把業務需求想清楚**
2. 🔑 **每份資料只存一份,需要時用 ID 參照**(避免冗餘與不一致)
3. 🔑 **PK 選跟業務無關的流水號,FK 跟它指向的 PK 同名**
4. 🔑 **NULL 是「沒有值」這件事本身,不是空字串、不是 0**
5. 🔑 **設計欄位的 NULL 設定 = 思考業務語意**(例如「庫存 = 0」和「庫存 = NULL」是不是兩回事?)
6. 🔑 **業務意義 = 把資料翻成人話**,這是溝通與設計的基礎能力

---

## 下一個主題

> **資料關聯 — 一對多關聯**
> 延伸今天的 PK / FK 觀念,正式介紹一對一、一對多、多對多三種關聯,以及 JOIN 的前置概念。
